#include "main.h"

/**
 * _abs - computes the absolute value of no
 * @n: the int to check no
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

