#include <stdio.h>
/**
 * main - entry point
 * description : printing number till 10
 * Return: always 0
 */
int main(void)
{
	int num;

	for (num == o; num < 10; num++)
	{
		putchar(num++);
	}
	putchar('\n');
	return (0);
}
