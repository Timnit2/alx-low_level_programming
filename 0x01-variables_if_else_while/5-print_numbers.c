#include <stdio.h>
/**
 * main  - entry point
 * description : printing base 10
 * Return: always 0
 */
int main(void)
{
	int btn = 0;

	while (btn < 10)
	{
		printf("here it is the decimal number base 10: %d", btn);
		btn++;
	}
	return (0);
}
